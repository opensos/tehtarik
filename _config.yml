title: TEH TARIK
description: A personal photo journal
baseurl: "/tehtarik"
url: "https://tehtarik.net"

# Build settings
markdown: kramdown
permalink: pretty

# Exclude from build
exclude:
  - README.md
  - Gemfile
  - Gemfile.lock

# Collections (optional — for future blog posts)
collections:
  posts:
    output: true
